# ⚠️ IMPORTANT — this repo is PUBLIC.
# Working notes and planning documents live in a separate private repo alongside it.
# No personal details, account names or billing identifiers belong here.
# These patterns are a safety net in case anything strays back in.
docs/
study/
private/
*.landing.html
*.explainer.html

# Secrets — never commit. `.env.example` is the documented template and IS tracked.
.env
.env.*
!.env.example

.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Corpora you supply yourself. `data/sample/` is original to this repo and IS tracked.
data/raw/
data/*.db
data/*.parquet

*.log
.DS_Store
clickhouse_data/
